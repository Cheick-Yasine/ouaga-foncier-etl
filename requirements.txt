# --- Scraping ---
playwright==1.47.0

# --- API LLM (structuration) ---
# Remplace l'API Claude (anthropic) sur demande du 2026-08-01 - gpt-4o-mini via
# Structured Outputs, voir config.py pour la justification du choix de modèle.
# Contrairement à anthropic==0.34.2, l'instanciation d'AsyncOpenAI ne plante PAS
# en présence d'un proxy SOCKS dans l'environnement (testé en conditions réelles
# avec ALL_PROXY défini) - pas besoin du paquet socksio ici.
openai==2.52.0

# --- Validation / parsing des données structurées ---
pydantic==2.9.2

# --- Base de données maître (PostgreSQL) ---
# psycopg (v3) avec l'extra [binary] : évite d'avoir besoin des en-têtes de
# compilation libpq sur la machine (CI comme poste local). Version installée
# et validée par exécution réelle de la suite de tests contre un vrai serveur
# PostgreSQL 16 le 2026-08-01 (upsert, types BOOLEAN/TIMESTAMPTZ, ON CONFLICT).
psycopg[binary]==3.3.4

# --- Lecture de la config des groupes (Groupe.xlsx -> groups.csv) ---
openpyxl==3.1.5

# --- Gestion des variables d'environnement en local ---
python-dotenv==1.0.1

# --- Tests ---
pytest==8.3.3
pytest-asyncio==0.24.0

# Versions validées par exécution réelle de `pip install -r requirements.txt` +
# `pytest -q` (139/139 tests passés, y compris contre un vrai serveur PostgreSQL 16)
# le 2026-08-01. playwright n'a PAS pu être testé en conditions réelles (pas d'accès
# à facebook.com depuis ce sandbox) - seul son installation/import a pu être
# vérifiée, pas le scraping live. openai a été installé et son instanciation
# vérifiée en conditions réelles, mais un vrai appel chat.completions.create()
# n'a PAS pu être testé (pas d'accès réseau sortant vers api.openai.com depuis
# ce sandbox) - à valider par un run réel avant tout usage à volume.
